package example;

public class StringUtils {

    /**
     * This is a comment.
     *
     * @param haystack the haystack
     * @param needle   can be dangerous
     * @return true if lucky
     */
    public boolean contains(String haystack, String needle) {
        return haystack.contains(needle);
    }
}
